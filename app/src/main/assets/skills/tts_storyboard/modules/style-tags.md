# 风格标签模块

当前引擎支持 `style_tags`。在不改变角色归因的前提下，为 `character/thought` 输出最多 4 个供应商无关的 `styleConcepts`，例如“温柔、低沉、活泼、克制、严肃、新闻播报”。

- 只根据当前场景和台词语气标注，不输出 Edge、火山等供应商的 style ID。
- 没有可靠风格信号时返回空数组。
- 旁白和 `other` 保持空数组。
