# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# CI runner has about 7 GB available. Balance Gradle and Kotlin compiler heaps.
org.gradle.jvmargs=-XX:+UseParallelGC -Xmx3g -Xms256m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
kotlin.daemon.jvmargs=-Xmx3g
org.gradle.workers.max=2
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects.
# org.gradle.parallel=true
# AndroidX package structure to make it clearer which packages are bundled with the
# AndroidX package structure to make it clearer which packages are bundled with the
# android.useAndroidX package.
android.useAndroidX=true
android.enableJetifier=false
kotlin.code.style=official
# kotlin.incremental.useClasspathSnapshot=true
android.enableResourceOptimizations=true
android.experimental.enableNewResourceShrinker.preciseShrinking=true
org.gradle.vfs.watch=true
org.gradle.unsafe.configuration-cache=false
android.defaults.buildfeatures.aidl=false
android.defaults.buildfeatures.buildconfig=false
android.defaults.buildfeatures.renderscript=false
android.defaults.buildfeatures.resvalues=false
android.defaults.buildfeatures.shaders=false
android.nonTransitiveRClass=true
CronetVersion=128.0.6613.40
CronetMainVersion=128.0.0.0
android.injected.testOnly=false
android.nonFinalResIds=true
android.injected.androidTest.leaveApksInstalledAfterRun=true
